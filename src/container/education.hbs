<div>
    {{#with main}}
     <main>
        <section>
            {{> title data='My education' }}
            <ul>
                {{#each education}}
                <li>
                     {{name}}
                    {{#unless isEnd}} (studying) {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>
                <section>
            {{> title data='My certificates' }}
            <ul>
                {{#each  certificates}}
                <li>
                     {{name}} (№{{id}})
                </li>
                {{/each}}
            </ul>
        </section>
     </main>

    {{/with}}

</div>